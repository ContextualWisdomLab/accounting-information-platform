# Test Strategy

## Test layers

1. **Value contracts:** decimal, currency, codes, references, hashes, line sidedness.
2. **Proposal invariants:** minimum lines, unique line numbers, exact balance.
3. **Policy resolution:** tenant, legal entity, book role, period, currency, and account mapping.
4. **Posting behavior:** idempotent replay, hash conflict, immutable receipt and journal, tenant-scoped idempotency and reversal caches, fail-closed overwrite of a posted `proposal_id` with a different idempotency key.
5. **Reversal behavior:** original preservation, equal-and-opposite lines, duplicate reversal idempotency.
6. **Trial balance:** scope and date filters, exact debit/credit equality, reversal net-zero fixture.
7. **Persistence:** clean migration, upgrade rehearsal, concurrent idempotency, transaction rollback, RLS.
8. **Contracts:** producer and consumer fixtures for proposal, policy, receipt, and outbox events, including Billing `proposal_status` ingest (`validated`/`exported` accept; `draft`/`rejected`/`posted` and operational reject rows fail closed).
9. **Security:** cross-tenant references, oversized payloads, malformed decimals, replay storms, unauthorized close or reversal.

## Merge gates

- Production statement coverage: 100%.
- Production branch coverage: 100%.
- Public API docstrings: 100% through `-Werror` documentation and package inspection.
- No skipped required test evidence.
- Exact-head CI, SAST, secret scan, dependency policy, SBOM, and provenance.
- Hash-locked coverage 7.15.4 wheels (including the CPython 3.13 manylinux x86_64 artifact) plus setuptools, wheel, packaging, psycopg, and psycopg-binary so `--require-hashes --only-binary=:all:` and `--no-build-isolation` packaging smoke tests resolve offline.
- PostgreSQL 18 integration evidence for a two-line post, idempotent replay, append-only reversal, trial-balance tie-out to the journal population, first-class period close with a durable snapshot, closed-period zero-row rejection, idempotent re-close, continued posting into a later open period, catalog policy resolution from a Billing `validated` proposal, a Billing `validated` cash receipt (`cash_receipt` debit / `accounts_receivable` credit) posted from catalog mapping with ingest+post+replay, HTTP `POST /journal-proposals` accept (catalog policy on the receipt, idempotent replay, and zero-row rejection for draft, operational reject, closed period, and cross-tenant; missing line keys, JSON-number/float amounts, bool/`"one"` `proposal_contract_version`, and Billing `retained_earnings` are 422; `POST /journal-proposals?trace=1` posts; body over 1 MiB is 413), HTTP `GET /posting-receipts` lookup by Billing invoice_draft or cash_receipt idempotency key (same receipt after post and replay; cross-tenant and unknown key write zero rows), HTTP `POST /period-closes` plus `GET /trial-balances` (omit `period_status_code` still hard-closes with snapshot TB after posted journals; empty or null `period_status_code` is 422 and does not hard-close, idempotent re-close, live TB on an open period, cross-tenant and missing period write zero rows, `GET /healthz` 200), HTTP two-step close on the same `POST /period-closes` (`period_status_code=soft_closed` writes no snapshot, rejects a later Billing invoice, accepts append-only reversal, live TB includes that reversal, re-soft-close is idempotent; omit then hard-closes and snapshots once; further reversal and `hard_closed` → `soft_closed` are 422 with zero extra rows; `GET /fiscal-periods` shows `soft_closed` then `hard_closed`; cross-tenant 403), Billing #15 pull (`pull_validated_journal_proposals` / `accept_pulled_proposals` / `POST /billing-proposal-pulls`) of invoice and cash `validated` proposals against a fake Billing WSGI/HTTP app (same receipt on `GET /posting-receipts`, replay writes zero extra journals, `draft`/`exported`/`rejected` wire items are not posted, an unknown-role validated row is `rejected_proposals` with `unknown_account_role` and writes zero journals, empty pull is `posting_receipts` [] plus `rejected_proposals` [], a repeating `next_cursor` and a 21-page list are 422, cross-tenant Billing 404 and AIS tenant mismatch write zero rows, missing `BILLING_BASE_URL` and Billing 422/5xx fail closed with a next action), HTTP `POST /journal-reversals` (append-only reverse of a posted invoice, original receipt lookup unchanged, reversing receipt at `reversal:{journal_reference}`, idempotent replay, cross-tenant 403, unknown journal or key and reverse after hard close write zero reversing journals), HTTP `GET /journal-reversals?legal_entity_reference=` (empty history is `journal_reversals` []; reverse a posted invoice and cash lists original plus reversal references, stored `reversal_date`, and `reversal_reason_code`; optional `original_journal_reference` returns one row; optional `fiscal_period_reference` joins the reversing journal period; `page_limit` default 50 maximum 100 with `next_cursor` `posted_at|journal_reference`; missing tenant header or entity query is 400; unknown entity or period is 404; unknown original is 200 []; cross-tenant 403 writes zero rows; POST reverse remains the command), HTTP `GET /period-closes?legal_entity_reference=` (empty history is `period_closes` []; soft-close writes no snapshot and GET stays []; hard-close then GET returns the same receipt fields; idempotent re-close still one snapshot; optional `fiscal_period_reference` returns that period; optional `period_status_code=soft_closed` is 200 [] and `hard_closed` lists stored snapshots; `page_limit` default 50 maximum 100 with `next_cursor` `snapshot_generated_at|snapshot_record_id`; missing tenant header or entity query is 400; unknown entity or period is 404; cross-tenant 403 writes zero rows; POST close remains the command), HTTP `GET /account-role-mappings` (seeded `accounts_receivable` 110100, `usage_revenue` 410100, `cash_receipt` 110200, `tax_payable` 210100, `retained_earnings` 310100, `write_off_expense` 510100, and `unapplied_cash` 210200 plus stored policy versions; cross-tenant 403 writes zero rows; unknown book or entity fails closed; `POST /account-role-mappings` 405), HTTP `GET /legal-entities` (seeded statutory entity plus a second current `legal_entity_record`; `legal_entity_reference` from `legal_entity_code` and stored `entity_name`; empty tenant returns `legal_entities` []; missing tenant header is 400; cross-tenant 403 writes zero rows; `POST /legal-entities` 405), HTTP `GET /accounting-books?legal_entity_reference=` (seeded `primary_statutory` book with `book_reference` / `accounting_book_reference` from `book_name` and `intended_book_role_code` `primary_statutory`; empty entity returns `accounting_books` []; unknown entity is 404; cross-tenant 403 writes zero rows; `POST /accounting-books` 405), HTTP `GET /chart-accounts?legal_entity_reference=&book_reference=` (seeded 110100 asset/debit, 410100 revenue/credit, 110200 asset/debit, 210100 liability/credit, 210200 liability/credit, 310100 equity/credit, 510100 expense/debit with `account_name`, `normal_balance_code`, and `account_class_code`; empty book returns `chart_accounts` []; unknown entity or book is 404; cross-tenant 403 writes zero rows; `POST /chart-accounts` 405), HTTP `GET /financial-statements?legal_entity_reference=&book_reference=&fiscal_period_reference=&statement_type_code=` (taxed invoice + cash + taxed-credit unwind: income statement shows usage_revenue 410100 only; balance sheet shows AR 110100, cash 110200, and tax_payable 210100 and ties with `net_income_amount`; empty period is `statement_lines` [] with zero totals; unknown period is 404; cross-tenant 403 writes zero rows; bad `statement_type_code` is 400; `POST /financial-statements` 405), HTTP `GET /financial-statements?statement_type_code=changes_in_equity` (empty books return opening/period_net_income/other/closing at zero; open August revenue plus issued-capital 320100 ties closing_equity to BS equity + NI and period_net_income to the income statement; soft-close stays live; after July then August hard-close, period_net_income stays pre-close P&L, closing_equity matches post-close BS equity, and the closing journal is not other_equity_movements; YTD opening is year start and comparison omits unused keys; unknown entity or period is 404; cross-tenant 403 writes zero rows), HTTP `GET /financial-statements?statement_type_code=cash_flow` (empty books return the eight IAS 7 indirect rows at zero; an untaxed invoice with no cash keeps closing_cash 0 and offsets net income with an AR working-capital use; invoice plus cash receipt makes cash_from_operations equal cash received and closing_cash equal the cash_receipt chart balance; after hard-close, period_net_income stays pre-close P&L, the closing journal is not working capital or financing, and closing_cash still ties to BS cash; September after August hard-close opens from the prior snapshot; YTD and comparison keep today's omit rules; unknown entity or period is 404; cross-tenant 403 writes zero rows), HTTP financial-statement comparison (optional `comparison_fiscal_period_reference`: August taxed invoice vs July 10000 invoice returns current and comparison lines plus both `net_income_amount` values; omit keeps today's keys; empty June comparison is `comparison_statement_lines` [] with zero comparison totals; hard-closed July comparison uses snapshot retained earnings and `comparison_net_income_amount` `0` while August stays live; unknown comparison period is 404; missing entity or book is 404; cross-tenant 403 writes zero rows), HTTP financial-statement year-to-date (optional `statement_scope_code`: omit or `period` is August-only P&L after a hard-closed July 10000 invoice plus live August taxed invoice; `year_to_date` August P&L is July+August earnings with closing journals excluded; `year_to_date` July BS matches period July BS with parked 310100; comparison uses the same scope; unknown scope is 400; cross-tenant 403 writes zero rows), HTTP hard-close closing entries (taxed invoice + cash + taxed-credit unwind plus remaining invoice: soft-close writes zero closing journals and no 310100 activity; hard-close posts one AIS closing journal, `GET /trial-balances` shows 410100 net zero and 310100 credit equal to pre-close earnings, income statement `net_income_amount` matches pre-close, balance sheet includes `retained_earnings` 310100 and `net_income_amount` `0`; re-hard-close keeps one closing journal; empty hard-close writes zero closing journals; Billing `retained_earnings` is rejected; `GET /chart-accounts` and `GET /account-role-mappings` include 310100 / `retained_earnings`; cross-tenant 403), Billing #19 taxed invoice (`tax_payable` → 210100 on the existing `invoice_draft` key; debit AR inclusive 110100 / credit revenue exclusive 410100 / credit tax payable 210100; untaxed two-line AR/revenue unchanged; missing mapping, replay, pull, and cross-tenant fail closed), Billing #20 taxed credit (existing `credit_adjustment` key; debit usage_revenue 410100 / debit tax_payable 210100 / credit AR 110100 from Billing's exact lines; untaxed two-line credit unchanged; AIS does not recompute the tax split; pull and cross-tenant fail closed), Billing #51 collection write-off (`write_off_expense` → 510100 on `{tenant}:collection_write_off:{collection_write_off_id}:{source_payload_hash}:v{version}`; debit write_off_expense 7000 / credit AR 7000; HTTP post, pull, receipt replay, GET by that key, and GET `/account-role-mappings` include the new row; unknown role still 422; income statement includes 510100; soft-close then replay is 200; hard-close clears 510100 to RE 310100 and `journal_source_code=billing` still lists the write-off, not the closer), Billing #59 unapplied-cash refund (`unapplied_cash` → 210200 on `{tenant}:unapplied_cash_refund:{unapplied_cash_refund_id}:{source_payload_hash}:v{version}`; debit unapplied_cash 8000 / credit cash_receipt 8000; Billing #60 leftover park uses `{tenant}:unapplied_cash:{unapplied_cash_id}:{source_payload_hash}:v{version}` with debit cash_receipt 3000 / credit unapplied_cash 3000 and replay of that key returns the original receipt; Billing #61 apply uses `{tenant}:unapplied_cash_application:{unapplied_cash_application_id}:{source_payload_hash}:v{version}` with debit unapplied_cash / credit AR, drops GET `/receivable-agings` by the applied amount, FIFO-clears the oldest AR debit, and excess apply credit uses `unapplied_credit_amount`; HTTP post, pull, receipt replay, GET `/account-role-mappings` include the new row; unknown role and Billing `retained_earnings` stay 422; soft-close then replay is 200 and a new refund is 422; hard-close leaves 210200 net 5000 and writes zero closing journals; GET `/payable-agings` stays 210100 and `chart_account_code=210200` is 422), HTTP `GET /unapplied-cash-rollforwards?legal_entity_reference=&book_reference=&fiscal_period_reference=` (empty leftover history returns parked/applied/refunded/opening/closing `0`; park 8000 then apply 3000 then refund 2000 is parked 8000 / applied 3000 / refunded 2000 / closing 3000 and equals account-balances 210200 credit−debit; soft-close stays live; hard-close keeps that closing and does not clear 210200; July park then hard-close makes August opening the prior snapshot; role-pair leftover journals without the published prefixes still classify; an unclassified adjusting 210200 line adds `other_movement_amount`; unknown entity, book, or period is 404; missing tenant 400; cross-tenant 403 writes zero rows; `POST /unapplied-cash-rollforwards` 405), HTTP `GET /journals` (posted invoice lines 110100/410100 by Billing idempotency key or journal reference; original key still returns the original journal after `POST /journal-reversals`; reversing key or reference returns the reversing lines; cross-tenant 403 writes zero rows; unknown key is 404), HTTP `POST /journals` (AIS-owned adjusting journal on open period posts balanced 110100/410100 lines and GET by `idempotency_key` returns them; soft-closed period accepts a later adjusting journal; hard-closed period is 409 with zero extra journals; replay of the same tenant plus key returns the existing receipt; unbalanced, unknown chart, `journal_date` outside the period, missing tenant header, and cross-tenant 403 write zero rows; Billing `POST /journal-proposals` and GET list fixtures stay green), HTTP `GET /journals?legal_entity_reference=&fiscal_period_reference=&book_reference=` (lists existing August invoice, cash, and taxed-credit journals and excludes a September journal; empty open period returns `journals` []; reverse appears on the list; `page_limit` default 50 maximum 100 with `next_cursor` `accounting_date|journal_reference`; missing entity/book/period is 404; cross-tenant 403 writes zero rows; single-key GET still returns lines), HTTP `GET /journals` optional `journal_source_code` (empty source filters return `journals` []; invoice plus cash plus adjusting: omit returns all three without the envelope key, `adjusting` returns only the adjusting journal, `billing` returns invoice and cash; after reverse, `reversal` returns the reversing journal and the original stays `billing`; after hard-close, `period_closing` returns exactly one closer and `billing` excludes it; invalid source is 400; single-journal GET by key or reference still returns lines), HTTP `POST /fiscal-periods` plus `GET /fiscal-periods` (open the next period and post into it, idempotent replay writes no second row, GET returns open status and dates, cross-tenant 403 writes zero rows, open of `hard_closed` fails closed, `POST /period-closes` still closes a period opened over HTTP), HTTP `GET /fiscal-periods?legal_entity_reference=` (lists existing August and a later opened period with `open` / `hard_closed` status; empty calendar returns `fiscal_periods` []; `page_limit` default 50 maximum 100 with `next_cursor` `period_start_date|period_code`; missing entity is 404; cross-tenant 403 writes zero rows; single GET by `fiscal_period_reference` and POST open still work), HTTP `GET /account-ledgers?legal_entity_reference=&chart_account_code=` (AR 110100 invoice + cash + credit lines and exact scope totals; tax_payable 210100 taxed invoice + credit unwind; empty cash 110200 is `ledger_lines` []; period filter excludes a later period; `page_limit` default 50 maximum 100 with `next_cursor` `posted_at|journal_reference|line_number`; unknown account or entity is 404; cross-tenant 403 writes zero rows; `POST /account-ledgers` 405), HTTP `GET /account-balances?legal_entity_reference=&book_reference=&fiscal_period_reference=` (empty books return `account_balances` []; invoice plus cash keeps AR 110100 remaining net 7000 and cash 110200 matching the cash-flow / BS fixtures with revenue 410100 credit; optional `chart_account_code` returns that one row; after soft-close an adjusting journal appears on the live balances; after hard-close 310100 appears from the snapshot and P&L nets to zero as on GET /trial-balances; `page_limit` default 50 maximum 100 with `next_cursor` `chart_account_code`; unknown entity, book, period, or chart code is 404; missing tenant 400; cross-tenant 403 writes zero rows; `POST /account-balances` 405), HTTP `GET /account-rollforwards?legal_entity_reference=&book_reference=&fiscal_period_reference=&chart_account_code=` (empty known 110100 returns zeros; invoice plus cash gives AR period debit 25000 / credit 18000 and cash period debit 18000 whose closing matches account-balances and cash-flow closing_cash; after soft-close an adjusting journal appears in period activity; after hard-close 310100 period includes the closing park and closing matches the snapshot; September opening reads the prior August snapshot; YTD adds `statement_scope_code` and explicit `period` omits it; unknown chart is 404; missing tenant 400; cross-tenant 403 writes zero rows; `POST /account-rollforwards` 405), HTTP `GET /receivable-agings?legal_entity_reference=&book_reference=&fiscal_period_reference=` (empty books return zero buckets; dated June/July/August invoices land in over-90 / 61-90 / 31-60 / current; FIFO cash consumes the oldest debit first; a credit adjustment continues FIFO; full cash apply returns zeros; invoice 25000 plus cash 18000 is current 7000 and equals account-balances 110100 net; after adjusting plus hard-close the total still equals snapshot net and the closer is excluded; a Billing #51 write-off after an invoice 25000 drops current and total from 25000 to 18000 and equals 110100 net; FIFO write-off consumes the oldest June debit first; a clearing write-off returns unsigned zeros; excess write-off credit adds `unapplied_credit_amount` and keeps buckets unsigned; soft-close rejects a later ordinary write-off; cash chart 422; unknown period 404; missing tenant 400; `POST /receivable-agings` 405), HTTP `GET /payable-agings?legal_entity_reference=&book_reference=&fiscal_period_reference=` (empty books and an untaxed invoice return zero buckets; a Billing three-line taxed invoice credits 210100 and ages that tax in current; a June taxed invoice plus an in-period taxed invoice split over-90 and current; FIFO tax-unwind consumes the oldest credit first and leaves the remainder in that bucket; full unwind returns zeros; after adjusting plus hard-close the total still equals snapshot 210100 net (`credit_amount` − `debit_amount`) and the closer is excluded; AR chart 422; unknown period 404; missing tenant 400; `POST /payable-agings` 405; close-package JSON includes `payable_aging` matching standalone GET), HTTP `GET /period-close-packages?legal_entity_reference=&book_reference=&fiscal_period_reference=` (open August after July hard-close plus invoice, cash, and a taxed invoice: `period_close` is null and the inner documents match `GET /fiscal-periods`, omit-basis `GET /trial-balances`, `GET /financial-statement-packages`, default `GET /receivable-agings`, default `GET /payable-agings`, and default `GET /unapplied-cash-rollforwards`; receivable aging total equals TB and account-balances 110100 net; payable aging matches standalone 210100 and stays present as zeros when tax activity is missing; leftover rollforward stays present as zeros when leftover history is missing and after park/apply/refund equals standalone leftover GET and account-balances 210200 credit−debit; soft-close sets `period_status_code` `soft_closed` and keeps `period_close` null with live payable aging; hard-close makes `period_close` the latest `GET /period-closes` item and TB is the snapshot while both agings still use the live-journal rule; YTD and comparison are forwarded only into the inner financial-statement package and omit rules stay unchanged; missing tenant 400; cross-tenant 403; unknown period 404; `POST /period-close-packages` 405), HTTP `GET /financial-statement-packages?legal_entity_reference=&book_reference=&fiscal_period_reference=` (empty books return four zero-amount statements; after a hard-closed July 10000 invoice plus August invoice and cash, the four inner documents match the four `GET /financial-statements` calls; income-statement net income equals changes-in-equity period_net_income; closing_equity equals balance-sheet equity plus unparked net income; cash_flow closing_cash equals cash_receipt 110200; after August hard-close those tie-outs still hold with RE parked and period P&L still pre-close; YTD adds `statement_scope_code` on the package only and inner documents match YTD GETs; comparison adds existing `comparison_*` keys on every inner statement; explicit `period` omits the package scope key; missing tenant 400; cross-tenant 403; unknown period 404; `POST /financial-statement-packages` 405), HTTP `GET /trial-balances` optional `balance_basis_code` (empty books stay 200 `lines` []; open invoice omit is live without the basis key and equals `unadjusted` and `adjusted`; after soft-close plus adjusting, omit and `adjusted` include the 1000 adjusting sides and `unadjusted` does not; `post_close` is 409 until hard-close; after hard-close omit and `post_close` match the snapshot with 310100 credit 26000 and 410100 net zero; `adjusted` still shows pre-close P&L plus adjusting and excludes the closing journal; `unadjusted` excludes both adjusting and closing; invalid basis is 400), HTTP `GET /outbox-events?event_type_code=` plus `POST /outbox-events/{outbox_event_id}/publish` (unpublished `posting_receipt` after POST invoice and cash; `GET /posting-receipts` still works; reverse enqueues `journal_reversal`; close enqueues `period_close`; `page_limit` default 50 maximum 100 with `next_cursor` `created_at|outbox_event_id`; empty list is `outbox_events` []; publish sets `published_at` and replay is idempotent; subsequent unpublished GET omits it; cross-tenant GET/publish 403 writes zero rows; unknown id 404; `POST /outbox-events` and GET publish path 405), HTTP fail-closed naive list cursors (GET `/account-ledgers`, `/journal-reversals`, `/period-closes`, `/outbox-events`, and `/audit-events` reject a timestamp with no UTC offset as 422), and HTTP `GET /audit-events` (empty history is `audit_events` []; post plus hard-close lists `posting_receipt` and `period_close` only; optional `event_type_code` filters those same codes; `page_limit` default 50 maximum 100 with the outbox `created_at|outbox_event_id` cursor; after publish the row stays on `/audit-events` with `published_at` set and disappears from unpublished `/outbox-events`; missing tenant header is 400; cross-tenant 403 writes zero rows; `POST /audit-events` 405).

## Required real-world fixtures

- Billing invoice with receivable, revenue, and tax.
- Billing cash receipt settling receivable (`cash_receipt` debit / `accounts_receivable` credit).
- Billing collection write-off (`write_off_expense` debit / `accounts_receivable` credit) that reduces entity receivable aging by that amount.
- Billing leftover-cash journals on one catalog: park `{tenant}:unapplied_cash:{unapplied_cash_id}:{source_payload_hash}:v{version}`, apply `{tenant}:unapplied_cash_application:{unapplied_cash_application_id}:{source_payload_hash}:v{version}`, and refund `{tenant}:unapplied_cash_refund:{unapplied_cash_refund_id}:{source_payload_hash}:v{version}`. Park then apply drops receivable aging by the applied amount; 210200 stays off payable aging and remains after hard-close. `GET /unapplied-cash-rollforwards` then proves parked − applied − refunded plus opening equals closing and equals `GET /account-balances` 210200 (`credit_amount` − `debit_amount`).
- Prepaid credit producing contract liability rather than immediate revenue.
- Provider payout with cash clearing and provider fee.
- Refund and chargeback correction.
- Closed-period hold and next-period adjustment.
- Duplicate proposal replay and conflicting payload reuse.
- Reversal and replacement.
- Cross-tenant account mapping attempt.
